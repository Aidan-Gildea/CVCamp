Todo:
Make an event in the output box. When the currentImage of the outputBox is set, it invokes the event. 

OutputBoxes add themselves to the public static list of output boxes
InputBoxes add themselves to the public static list of input boxes

When an output box adds itself into the list, it iterates through all input boxes and subscribes them to its event
Input boxes have an event inside their CurrentImage that invokes their ImageChanged event. 
Subscribe an event inside each control to the inputbox imagechanged, so that when the image changes, 
it calls the event that does the DoWork function of the control.

All controls inherit from the CVIOBase 

EventArgs is generic, so you can put Mat inside public event EventArgs<Mat> myEvent; 

For the Add of the outputBox, you have the event outputNameChanged<OutputNameChanged> with a generic of a class
that holds oldname, newname and mat. This is so that it can assign the newname and kill the old one in the dict. 

